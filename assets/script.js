const menu=document.querySelector('.menu-toggle'), nav=document.querySelector('nav');menu?.addEventListener('click',()=>{const open=nav.classList.toggle('open');menu.setAttribute('aria-expanded',open);menu.textContent=open?'×':'☰'});document.querySelector('.theme-toggle')?.addEventListener('click',()=>document.body.classList.toggle('dark'));const topBtn=document.querySelector('.to-top');window.addEventListener('scroll',()=>topBtn?.classList.toggle('show',scrollY>500));topBtn?.addEventListener('click',()=>scrollTo({top:0,behavior:'smooth'}));const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');if(e.target.dataset.count){let n=0,end=+e.target.dataset.count,t=setInterval(()=>{n+=Math.ceil(end/45);e.target.textContent=Math.min(n,end)+(end===98?'%':end===1200?'+':'');if(n>=end)clearInterval(t)},25)}}}),{threshold:.15});document.querySelectorAll('.reveal,.stat-grid strong').forEach(el=>observer.observe(el));document.querySelectorAll('[data-filter]').forEach(btn=>btn.addEventListener('click',()=>{document.querySelectorAll('[data-filter]').forEach(b=>b.classList.remove('active'));btn.classList.add('active');document.querySelectorAll('.gallery-grid figure').forEach(f=>f.style.display=(btn.dataset.filter==='all'||f.dataset.category===btn.dataset.filter)?'block':'none')}));
